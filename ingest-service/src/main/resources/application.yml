server:
  port: 8081

spring:
  application:
    name: ingest-service
  kafka:
    bootstrap-servers: localhost:9092

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
