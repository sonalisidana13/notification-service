server:
  port: 8083

spring:
  application:
    name: dispatcher-service
  kafka:
    bootstrap-servers: localhost:9092

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
