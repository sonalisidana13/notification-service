server:
  port: 8082

spring:
  application:
    name: enricher-service
  kafka:
    bootstrap-servers: localhost:9092

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
